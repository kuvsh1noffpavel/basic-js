import { NotImplementedError } from '../extensions/index.js';

/**
 * Implement chainMaker object according to task description
 * 
 */


export default {

/*

  memoryChain : [],
  getLength() {
    return this.memoryChain.length;
  },
  addLink(value) {
    if(value === undefined){
      value = "";
    }
    this.memoryChain.push(`( ${value} )`);
    return this;
  },
  removeLink(position) {
    if (typeof position !== 'number' || (position^0) !== position || position < 0 || position > this.getLength()){
      this.memoryChain.splice(0);
      throw new Error ('Check the chain position');
    }
    let removePosition = position - 1;
    this.memoryChain.splice(removePosition, 1);
    return this;
  },
  reverseChain() {
    this.memoryChain.reverse();
    return this;
  },
  finishChain() {
    const finalChain = this.memoryChain.join('~~');
    this.memoryChain.splice(0);
    return finalChain;
  }

*/

};
